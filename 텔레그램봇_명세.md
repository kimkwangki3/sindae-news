# 해룡신문 — 텔레그램 봇 / 자동 기사·알림 명세 (VSCode 작업용)
_기존 빌드 위에 추가. `개발계획서.md`·`기자기능_추가명세.md`·`관리자_리뉴얼_명세.md` 함께 참고._

## 목표
1. **자동 기사 초안 → 텔레그램 승인 → 발행** (내 승인 한 번이면 게시)
2. **신규 이벤트 알림**(댓글 제외): 기사·업체등록·광고신청·단체등록·기자신청·제보·신고 등 들어오면 텔레그램으로 먼저 보고
3. **알림 모드 선택**: 실시간 / **예약(다이제스트)** — 정해진 시간에 모아서 발송(물량 많을 때)

## 준비물 (3개)
- 텔레그램 **봇 토큰** (@BotFather)
- 수신 **chat_id** (개인 또는 관리 그룹)
- 발행용 **Supabase service role key** (서버 전용)

## 아키텍처
- Next.js **API 라우트**(서버) + Supabase + 텔레그램 Bot API
- `POST /api/telegram/webhook` — 버튼 클릭·명령 수신
- `GET /api/cron/draft` — 기사 자동 수집·초안 (스케줄)
- `GET /api/cron/digest` — 예약 알림 발송 (스케줄)
- 스케줄: Vercel Cron(또는 외부 cron) → 위 엔드포인트 호출
- 비밀키는 `.env`(서버), webhook 은 `secret_token` 으로 위변조 방지

---

## 기능 A — 자동 기사 초안 → 승인 발행
### 수집·작성
- 시간대 지정(예: 매일 07:00, 12:00). 한 번에 **10개** 초안.
- 우선순위: **신대지구 → 없으면 해룡면 → 없으면 순천시**.
- 소스: 순천시청 보도자료·공공 공지 등 **인용·재구성 가능한 1차 자료** 중심. 타 언론 기사 복붙 금지(저작권). 사실 기반 새 작성 + **출처 표기**.
- 초안 저장: `news_drafts`(아래). 사이트엔 미노출.

### 텔레그램 전송 포맷 (초안 1건/메시지)
```
📝 [초안 3/10] 지역소식
제목: 신대지구 ○○ 공사 다음 달 착수
요약: (2~3줄)
본문: (전문 일부 미리보기)
출처: 순천시청 보도자료(링크)
[✅ 발행] [✏️ 수정] [🗑 보류]
```
### 버튼 동작 (webhook)
- **✅ 발행** → `articles` insert (`status='published'`, `published_at=now`, `origin='ai'`, `byline='편집부'`(또는 내 계정), `source_url`) → "게시 완료 + 링크" 회신
- **✏️ 수정** → 텔레그램에서 수정 지시 주고받아 초안 갱신 후 재전송 → 발행
- **🗑 보류** → `news_drafts.status='discarded'`
- 발행 책임: 최종 승인자(나/편집부). 바이라인·출처 항상 표기.

---

## 기능 B — 신규 이벤트 알림 (댓글 제외)
### 알림 대상 이벤트 (기본 ON, 개별 토글 가능)
| 이벤트 | 트리거 | 빠른 액션 버튼 |
|---|---|---|
| 기사 발행/승인요청(준기자) | articles insert | [승인][보기] |
| 기자 신청 | reporter_applications | [승인][반려] |
| 업체 등록 | businesses(pending) | [승인][반려] |
| 지역단체 등록 | organizations(pending) | [승인][반려] |
| 단체 가입신청 | org_members(pending) | [보기] |
| 광고 신청 | ad_requests | [승인][반려] |
| 제보 접수 | tips | [보기] |
| 신고 접수 | reports | [처리][보기] |
| (선택) 나눔마켓/게시판 신규글 | market_posts/board_posts | [보기] |
- **댓글은 제외**(요청).
- 빠른 액션 버튼으로 텔레그램에서 바로 승인/반려 가능(관리자 권한 확인 후 처리 + 감사로그 기록).
- 메시지엔 핵심 요약 + 관리자 페이지 딥링크.

### 구현
- 각 테이블 insert 시 알림 발생 → **Supabase DB 트리거 → Edge Function**, 또는 앱 서버 액션에서 `enqueueNotification()` 호출 → `notification_outbox` 적재 → 발송.

---

## 기능 C — 알림 모드 (실시간 / 예약 다이제스트)
- **실시간**: 이벤트 즉시 발송.
- **예약(다이제스트)**: 정한 시간에만 모아서 발송. 예) 09:00·14:00·19:00. 그 사이 이벤트는 큐에 쌓였다가 묶음 1건으로 요약 전송.
  ```
  🔔 14:00 알림 요약
  • 기사 신규 3 • 업체 등록 1 • 광고 신청 2 • 제보 1
  [관리자에서 보기]
  ```
- **조용시간(quiet hours)**: 예 22:00~07:00 발송 보류(긴급 제외).
- **유형별 모드**: 신고/제보는 실시간, 나머지는 다이제스트 등 개별 설정 가능.
- 설정 화면: `/admin/settings` 의 "텔레그램 알림" 섹션(superadmin).

---

## DB 추가 (마이그레이션)
```sql
-- 자동 기사 초안
create table if not exists news_drafts (
  id          uuid primary key default gen_random_uuid(),
  title       text not null,
  summary     text,
  body        text,
  category    text,
  source_url  text,
  scope       text,           -- 신대지구 / 해룡면 / 순천시
  status      text not null default 'pending',  -- pending/published/discarded/editing
  created_at  timestamptz not null default now()
);

-- 텔레그램/알림 설정 (단일 행 또는 관리자별)
create table if not exists telegram_config (
  id           smallint primary key default 1,
  bot_enabled  boolean not null default true,
  chat_id      text,                         -- 수신 대상
  mode         text not null default 'realtime', -- realtime / digest
  digest_times text[] default '{09:00,14:00,19:00}',
  quiet_start  time,                          -- 예 22:00
  quiet_end    time,                          -- 예 07:00
  event_toggles jsonb default '{}'            -- 유형별 on/off·모드
);

-- 발송 큐/로그
create table if not exists notification_outbox (
  id          bigint generated always as identity primary key,
  event_type  text not null,                 -- new_article / new_business / ...
  payload     jsonb,
  status      text not null default 'queued',-- queued/sent/failed
  scheduled_for timestamptz,                  -- 다이제스트 발송 예정
  sent_at     timestamptz,
  created_at  timestamptz not null default now()
);
create index on notification_outbox (status, scheduled_for);
```

## 환경변수 추가 (`.env.local`)
```
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_WEBHOOK_SECRET=
CRON_SECRET=
```

## 보안·법적
- service key·봇 토큰은 **서버 전용**. webhook 은 secret 검증.
- 텔레그램 빠른 액션도 **관리자 권한 확인** 후 처리, `admin_audit_logs` 기록.
- 자동 기사: 타 언론 복제 금지, 사실 기반 작성 + 출처 표기, 발행 책임=승인자.

## 완료 체크리스트
- [ ] 봇 생성·webhook 등록, `.env` 설정
- [ ] `/api/telegram/webhook` 버튼 처리(발행/수정/보류, 승인/반려)
- [ ] `/api/cron/draft` 수집·초안 10개 → 텔레그램 전송
- [ ] 발행 API: 승인 시 `articles` 게시(바이라인·출처)
- [ ] 신규 이벤트 알림(댓글 제외) + 빠른 액션
- [ ] 알림 모드: 실시간/다이제스트/조용시간 + 유형별 토글
- [ ] `news_drafts`·`telegram_config`·`notification_outbox` 마이그레이션
- [ ] 모든 텔레그램 처리 → 감사로그

## Claude Code 시작 프롬프트
> "`텔레그램봇_명세.md` 를 읽고 구현해줘. 먼저 마이그레이션과 `.env`·webhook 셋업, 그다음 기능 B(신규 이벤트 알림, 댓글 제외)부터 붙이고, 기능 A(자동 기사 초안→승인 발행), 마지막에 기능 C(실시간/다이제스트/조용시간)를 추가해줘. 모든 텔레그램 액션은 관리자 권한 확인 + 감사로그로 남겨줘."
